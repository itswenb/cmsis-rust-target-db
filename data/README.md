# 生成数据

`.github/workflows/update-data.yml` 会在此目录生成：

- `devices.jsonl`
- `devices.csv`
- `metadata.json`

数据来自生成时公开索引中的活跃 pack；已标记为 deprecated 的 pack 不在当前覆盖范围内。根目录 MIT 许可证仅覆盖仓库自有代码和自编内容，不改变上游权利，本仓库也不另行声明生成数据的许可证。详情见 [`DATA_PROVENANCE.md`](../DATA_PROVENANCE.md)。
